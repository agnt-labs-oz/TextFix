<Window x:Class="TextFix.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        x:Name="OverlayWin">

    <Window.Resources>
        <Style x:Key="PillBorder" TargetType="Border">
            <Setter Property="Background" Value="#E61A1A2E"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" Opacity="0.4" ShadowDepth="4"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SmallActionButton" TargetType="Button">
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.7"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PinButton" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="ToolTip" Value="Pin overlay open"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <!-- Dark ComboBox for mode selector in overlay -->
        <ControlTemplate x:Key="OverlayComboBoxToggle" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="18"/>
                </Grid.ColumnDefinitions>
                <Border x:Name="Border" Grid.ColumnSpan="2" Background="#2A2A3E"
                        BorderBrush="#555" BorderThickness="1" CornerRadius="3"/>
                <Path x:Name="Arrow" Grid.Column="1" Fill="#AAAAAA"
                      HorizontalAlignment="Center" VerticalAlignment="Center"
                      Data="M 0 0 L 3 3 L 6 0 Z"/>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Border" Property="BorderBrush" Value="#6C63FF"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
        <Style x:Key="OverlayComboBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="Background" Value="#2A2A3E"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Template="{StaticResource OverlayComboBoxToggle}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Focusable="False" ClickMode="Press"/>
                            <ContentPresenter IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              Margin="6,0,20,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#E0E0E0"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom"
                                   Focusable="False" AllowsTransparency="True" PopupAnimation="Slide">
                                <Border Background="#2A2A3E" BorderBrush="#555" BorderThickness="1"
                                        CornerRadius="3" Margin="0,2,0,0"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer>
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="OverlayComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#2A2A3E"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#6C63FF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3D3D5F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Storyboard x:Key="FadeOut">
            <DoubleAnimation Storyboard.TargetName="OverlayWin"
                             Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.25"/>
        </Storyboard>
    </Window.Resources>

    <Border x:Name="MainBorder" Style="{StaticResource PillBorder}" MaxWidth="420"
            MouseLeftButtonDown="OnDragMove">
        <StackPanel>
            <!-- Processing state: spinner + "Correcting..." -->
            <StackPanel x:Name="ProcessingPanel" Orientation="Horizontal" Visibility="Visible">
                <Ellipse x:Name="Spinner" Width="18" Height="18"
                         Stroke="#6C63FF" StrokeThickness="2"
                         StrokeDashArray="3 2" Margin="0,0,12,0">
                    <Ellipse.RenderTransform>
                        <RotateTransform CenterX="9" CenterY="9"/>
                    </Ellipse.RenderTransform>
                </Ellipse>
                <TextBlock Text="Correcting..." Foreground="#E0E0E0"
                           FontFamily="Segoe UI" FontSize="13" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Result state: diff + buttons + mode selector -->
            <StackPanel x:Name="ResultPanel" Visibility="Collapsed">
                <!-- Header row: status + pin toggle -->
                <DockPanel Margin="0,0,0,8">
                    <TextBlock x:Name="PinToggle" Style="{StaticResource PinButton}"
                               DockPanel.Dock="Right"
                               MouseLeftButtonDown="OnPinToggle"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="StatusIcon" Text="&#x2713;" Foreground="#4ADE80"
                                   FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="StatusText" Text="" Foreground="#E0E0E0"
                                   FontFamily="Segoe UI" FontSize="13" VerticalAlignment="Center"/>
                    </StackPanel>
                </DockPanel>

                <!-- Original text (strikethrough) -->
                <TextBlock x:Name="OriginalText" Foreground="#AAAAAA"
                           FontFamily="Segoe UI" FontSize="12" TextWrapping="Wrap"
                           TextDecorations="Strikethrough" Margin="0,0,0,4"/>
                <!-- Corrected text -->
                <TextBlock x:Name="CorrectedText" Foreground="#E0E0E0"
                           FontFamily="Segoe UI" FontSize="12" TextWrapping="Wrap"
                           Margin="0,0,0,10"/>

                <!-- Action buttons -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <Button x:Name="ApplyButton" Content="&#x2714; Apply (Enter)"
                            Background="#6C63FF" Foreground="White"
                            Style="{StaticResource ActionButton}" Click="OnApplyClick"/>
                    <Button x:Name="CancelButton" Content="&#x2718; Cancel (Esc)"
                            Background="#444444" Foreground="#CCCCCC"
                            Style="{StaticResource ActionButton}" Click="OnCancelClick"/>
                </StackPanel>

                <!-- Mode selector row -->
                <DockPanel Margin="0,4,0,0">
                    <TextBlock Text="Mode:" Foreground="#888" FontSize="11"
                               FontFamily="Segoe UI" VerticalAlignment="Center"
                               Margin="0,0,6,0" DockPanel.Dock="Left"/>
                    <ComboBox x:Name="ModeBox" Style="{StaticResource OverlayComboBox}"
                              SelectionChanged="OnModeChanged"/>
                </DockPanel>

                <!-- Auto-apply countdown -->
                <TextBlock x:Name="CountdownText" Foreground="#666666"
                           FontFamily="Segoe UI" FontSize="10" Margin="0,6,0,0"/>
            </StackPanel>

            <!-- Error state: message + action buttons -->
            <StackPanel x:Name="ErrorPanel" Visibility="Collapsed">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="&#x26A0;" Foreground="#FBBF24"
                               FontSize="16" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="ErrorText" Foreground="#E0E0E0"
                               FontFamily="Segoe UI" FontSize="13" VerticalAlignment="Center"
                               TextWrapping="Wrap" MaxWidth="300"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button Content="&#x21BB; Retry (Enter)"
                            Background="#6C63FF" Foreground="White"
                            Style="{StaticResource ActionButton}" Click="OnRetryClick"/>
                    <Button Content="Dismiss (Esc)"
                            Background="#444444" Foreground="#CCCCCC"
                            Style="{StaticResource ActionButton}" Click="OnDismissClick"/>
                </StackPanel>
            </StackPanel>

            <!-- Info state: no changes, focus lost, or applied confirmation -->
            <StackPanel x:Name="InfoPanel" Visibility="Collapsed">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="InfoIcon" Text="&#x2713;" Foreground="#4ADE80"
                               FontSize="16" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="InfoText" Foreground="#E0E0E0"
                               FontFamily="Segoe UI" FontSize="13" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock x:Name="InfoHint" Foreground="#666666"
                           FontFamily="Segoe UI" FontSize="10" Margin="0,6,0,0"
                           Visibility="Collapsed"/>
            </StackPanel>

            <!-- Idle state: opened from tray, no active correction -->
            <StackPanel x:Name="IdlePanel" Visibility="Collapsed">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                    <TextBlock Text="Tx" Foreground="#6C63FF"
                               FontSize="16" FontWeight="Bold" FontFamily="Segoe UI"
                               Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="TextFix" Foreground="#E0E0E0"
                               FontFamily="Segoe UI" FontSize="14" FontWeight="SemiBold"
                               VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock x:Name="IdleStatsText" Foreground="#666666"
                           FontFamily="Segoe UI" FontSize="11" Margin="0,0,0,4"/>
            </StackPanel>

            <!-- History panel: toggled by History button in action row -->
            <StackPanel x:Name="HistoryPanel" Visibility="Collapsed" Margin="0,8,0,0">
                <DockPanel Margin="0,0,0,6">
                    <TextBlock x:Name="HistoryStatsText" Foreground="#666"
                               FontFamily="Segoe UI" FontSize="11"
                               DockPanel.Dock="Right" VerticalAlignment="Center"/>
                    <TextBlock Text="Recent corrections" Foreground="#E0E0E0"
                               FontFamily="Segoe UI" FontSize="12" VerticalAlignment="Center"/>
                </DockPanel>
                <ScrollViewer MaxHeight="160" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="HistoryList"/>
                </ScrollViewer>
            </StackPanel>

            <!-- Custom prompt box -->
            <DockPanel x:Name="PromptPanel" Visibility="Collapsed" Margin="0,8,0,0">
                <Button x:Name="PromptGoButton" Content="Go" DockPanel.Dock="Right"
                        Background="#6C63FF" Foreground="White" Margin="4,0,0,0"
                        Style="{StaticResource SmallActionButton}" Click="OnPromptGo"/>
                <Grid>
                    <TextBox x:Name="PromptBox"
                             Background="#2A2A3E" Foreground="#E0E0E0" BorderBrush="#555"
                             CaretBrush="#E0E0E0" FontFamily="Segoe UI" FontSize="11"
                             Padding="6,3" VerticalAlignment="Center"
                             KeyDown="OnPromptKeyDown"
                             TextChanged="OnPromptTextChanged"
                             GotFocus="OnPromptFocusChanged"
                             LostFocus="OnPromptFocusChanged"/>
                    <TextBlock x:Name="PromptPlaceholder"
                               Text="Try a custom prompt..."
                               Foreground="#666666" FontFamily="Segoe UI" FontSize="11"
                               Padding="8,4" VerticalAlignment="Center"
                               IsHitTestVisible="False"/>
                </Grid>
            </DockPanel>

            <!-- Save as mode (shown after custom prompt success) -->
            <StackPanel x:Name="SaveModePanel" Orientation="Horizontal"
                        Visibility="Collapsed" Margin="0,4,0,0">
                <TextBlock x:Name="SaveModeLink" Text="Save as mode..."
                           Foreground="#6C63FF" FontSize="11" FontFamily="Segoe UI"
                           Cursor="Hand" VerticalAlignment="Center"
                           MouseLeftButtonDown="OnSaveModeClick"/>
                <TextBox x:Name="SaveModeNameBox" Visibility="Collapsed"
                         Background="#2A2A3E" Foreground="#E0E0E0" BorderBrush="#555"
                         CaretBrush="#E0E0E0" FontFamily="Segoe UI" FontSize="11"
                         Padding="6,3" Width="140" Margin="4,0,0,0"
                         KeyDown="OnSaveModeNameKeyDown"/>
            </StackPanel>

            <!-- Action row: redo original, redo refine, copy, history -->
            <StackPanel x:Name="ActionRowPanel" Orientation="Horizontal"
                        Visibility="Collapsed" Margin="0,8,0,0">
                <Button x:Name="RedoOrigButton" Content="&#x21BB; Original"
                        Background="#2A2A3E" Foreground="#AAAAAA"
                        Style="{StaticResource SmallActionButton}" Click="OnActionRedoOriginal"/>
                <Button x:Name="RedoRefineButton" Content="&#x21BB; Refine"
                        Background="#2A2A3E" Foreground="#AAAAAA"
                        Style="{StaticResource SmallActionButton}" Click="OnActionRedoRefine"/>
                <Button x:Name="CopyButton" Content="&#x2398; Copy"
                        Background="#2A2A3E" Foreground="#AAAAAA"
                        Style="{StaticResource SmallActionButton}" Click="OnActionCopy"/>
                <Button x:Name="HistoryToggleButton" Content="&#x29D6; History"
                        Background="#2A2A3E" Foreground="#AAAAAA"
                        Style="{StaticResource SmallActionButton}" Click="OnActionHistoryToggle"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
