<Window x:Class="TextFix.Views.CustomModeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Custom Mode"
        Width="400" Height="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E2E"
        Foreground="#E0E0E0"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D3F"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="CaretBrush" Value="#E0E0E0"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Margin" Value="0,4,0,12"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Custom Mode" FontSize="16" FontWeight="SemiBold"
                   Foreground="#E0E0E0" Margin="0,0,0,14"/>

        <StackPanel Grid.Row="1">
            <Label Content="Mode Name"/>
            <TextBox x:Name="NameBox"/>
        </StackPanel>

        <Label Grid.Row="2" Content="System Prompt"/>
        <TextBox Grid.Row="3" x:Name="PromptBox"
                 AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,4,0,12" VerticalContentAlignment="Top"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="72" Height="28"
                    Background="#6C63FF" Foreground="White" BorderBrush="Transparent"
                    FontSize="13" Cursor="Hand" Click="OnSave" Margin="0,0,8,0"/>
            <Button Content="Cancel" Width="72" Height="28"
                    Background="#444" Foreground="#CCC" BorderBrush="Transparent"
                    FontSize="13" Cursor="Hand" Click="OnCancel"/>
        </StackPanel>
    </Grid>
</Window>
